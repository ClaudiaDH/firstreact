import React from 'react';

class TableProfissionais extends React.Component{
    //sempre que tiver uma classe de React Component chamados a função render que retornará um HTML.
    render(){
        return(
            
                <h2>{this.props.textoDois}</h2>
                
                
            
        )
    }
}

export default TableProfissionais;
